load("//bazel_port:cmake.bzl", "cmake_build")

cmake_build(
    name = "cmake_build",
    srcs = ["@llama_cpp//:all_srcs"],
    build_targets = ["llama-cli"],
    cache_entries = [
        "LLAMA_BUILD_TESTS=ON",
        "LLAMA_BUILD_UI=OFF",
        "GGML_CCACHE=OFF",
    ],
    image = "//toolchains/container:cmake_image",
    jobs = 2,
)
